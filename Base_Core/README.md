# Project Setup & Useful Commands

Reusable base library for all Python projects in the MilnerLab.  
It contains common functionality for fitting, smoothing and model-related utilities that can be shared across multiple applications.

---

## 1. Virtual Environment

In this project no virtual environment is needed, because it only serves as a storage space for base implementations.

Dependencies will be loaded from the venv that is used to compile the code.