# Project Setup & Useful Commands

This repository contains the code for connecting to and using devices in the MilnerLab.
It only implements the devices, not the code that would be used to interact with the devices.

---

## 1. Virtual Environment

### 1.1 Creates / checks / activates environments

Use the provided PowerShell script:

```powershell
. .\setup_env.ps1
```

Notes:
- this script creates the `x32` venv that's needed in order to run the spm002.






